{
  "bitbucket": {
    "_comment": "Bitbucket Pipelines OIDC. THREE different UUIDs are involved - see the 'Pipeline deploys' section of doc/infrastructure.md. Run the show-oidc-claims custom pipeline to print the exact values rather than assembling them by hand. None of this is secret; they are identifiers.",
    "workspace": "fiskl",
    "audience": "FILL-ME  Repository settings -> OpenID Connect, the Audience field"
  },
  "test": {
    "account": "355369890944",
    "region": "us-east-1",
    "domainName": "test-help.fiskl.com",
    "profiles": [
      "fiskl-help-test",
      "stage-admin"
    ],
    "basicAuth": true,
    "noindex": true,
    "retainBucket": false,
    "ciDeployRole": true,
    "oidcSubject": "FILL-ME  run the show-oidc-claims pipeline; braces are literal"
  },
  "prod": {
    "account": "396752440154",
    "region": "us-east-1",
    "domainName": "help.fiskl.com",
    "profiles": [
      "fiskl-help-prod",
      "prod-release"
    ],
    "basicAuth": false,
    "noindex": false,
    "retainBucket": true,
    "ciDeployRole": true,
    "oidcSubject": "FILL-ME  run the show-oidc-claims pipeline; braces are literal"
  }
}
